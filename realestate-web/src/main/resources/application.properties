# ===================================================================
# 后端服务配置（R-004 阶段 0）
# ===================================================================

spring.application.name=realestate-web
server.port=8080

# 数据库与连接池在第 1 阶段接入：那时会把 core 的 DatabaseUtil.getConnection
# 换成 HikariCP，事务改为 Spring 声明式事务，才会出现 spring.datasource.* 配置。
# 现阶段后端不连库，core 只是被依赖进来。
